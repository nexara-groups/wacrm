/**
 * Demo data for the conversations slice.
 *
 * Empty on purpose: the conversations screens are not built yet, and an empty
 * seeder says so honestly. When the slice lands, fill this in THROUGH THE
 * REPOSITORIES (see ./types.ts) — never with raw inserts that the
 * application layer could not have produced.
 */
import type { SeedContext } from "./types";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function seedConversations(_ctx: SeedContext): Promise<void> {
  // Intentionally empty — see the module docstring.
}
